[package]
name = "kineto-job-runtime"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
kineto-jobs = { path = "../kineto-jobs" }
kineto-project = { path = "../kineto-project" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
path = "src/lib.rs"
